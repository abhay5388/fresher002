<!DOCTYPE html>
<html>
<head>
    <style>
        body { background-color: #ffebee; display: flex; justify-content: center; align-items: center; height: 100vh; font-family: 'Arial', sans-serif; }
        
        /* The Animation */
        .proposal-card {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
            transform: scale(0);
            animation: popIn 1s cubic-bezier(0.17, 0.89, 0.32, 1.49) forwards;
        }

        @keyframes popIn {
            to { transform: scale(1); }
        }

        h1 { color: #d81b60; }
        .buttons { margin-top: 20px; }
        button { 
            padding: 10px 25px; 
            font-size: 18px; 
            cursor: pointer; 
            border: none; 
            border-radius: 10px; 
            margin: 0 10px;
            transition: 0.3s;
        }
        .yes { background-color: #4caf50; color: white; }
        .no { background-color: #f44336; color: white; }
        button:hover { transform: scale(1.1); }
    </style>
</head>
<body>

    <div class="proposal-card">
        <h1>Will you go to Prom with me? 💃🕺</h1>
        <div class="buttons">
            <button class="yes" onclick="alert('Yay! See you there! ❤️')">YES!</button>
            <button class="no" onmouseover="moveButton(this)">No</button>
        </div>
    </div>

    <script>
        // Fun trick: Make the 'No' button move when they try to click it
        function moveButton(btn) {
            const x = Math.random() * (window.innerWidth - btn.offsetWidth);
            const y = Math.random() * (window.innerHeight - btn.offsetHeight);
            btn.style.position = 'absolute';
            btn.style.left = x + 'px';
            btn.style.top = y + 'px';
        }
    </script>

</body>
</html>